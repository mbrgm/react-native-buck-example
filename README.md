# react-native-buck-example

For OSX, the Android support repository is supposed to be available at
`/usr/local/opt/android-sdk/extras/android/m2repository`.

Then, to get started:

```
$ npm install
$ buck fetch android:app
$ buck install android:app
```
